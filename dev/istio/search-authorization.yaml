apiVersion: security.istio.io/v1
kind: AuthorizationPolicy
metadata:
  name: search-allow-selected-services
  namespace: dev
spec:
  selector:
    matchLabels:
      app.kubernetes.io/name: search
  action: ALLOW
  rules:
    - from:
        - source:
            principals:
              - cluster.local/ns/dev/sa/storefront-bff
              - cluster.local/ns/dev/sa/backoffice-bff
              - cluster.local/ns/dev/sa/product
              # ingress-nginx can duoc phep de api.dev.local.com/search hoat dong
              # (xem README muc 3.1). Kiem tra ten SA: kubectl get sa -n ingress-nginx
              - cluster.local/ns/ingress-nginx/sa/ingress-nginx
      to:
        - operation:
            ports:
              - "80"
    - to:
        - operation:
            ports:
              - "8090"

