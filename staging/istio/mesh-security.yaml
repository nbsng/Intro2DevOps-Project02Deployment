apiVersion: security.istio.io/v1
kind: PeerAuthentication
metadata:
  name: staging-mtls-strict
  namespace: staging
# KHONG them DestinationRule wildcard ISTIO_MUTUAL o day - se lam hong ket noi
# toi Postgres/Kafka/Redis/ES ngoai mesh. Xem giai thich chi tiet trong
# dev/istio/mesh-security.yaml. Auto-mTLS + STRICT la du.
spec:
  mtls:
    mode: STRICT
